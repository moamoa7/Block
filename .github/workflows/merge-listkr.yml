name: Convert List-KR classic (uBO)
on:
  schedule:
    - cron: '0 */6 * * *'   # 6시간마다
  workflow_dispatch:

permissions:
  contents: write

jobs:
  convert:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Fetch & convert
        run: |
          SRC="https://cdn.jsdelivr.net/npm/@list-kr/filterslists@latest/dist/filterslist-uBlockOrigin-classic.txt"
          {
            echo "[Adblock Plus 2.0]"
            echo "! Title: List-KR filters list Classic for uBlock Origin"
            echo "! Description: uBlock Origin용 List-KR 클래식 필터 리스트입니다. 한국어권 웹 페이지 및 앱에 있는 광고와 거부되지 않은 안티 애드블록을 차단합니다."
            echo "! Updated: $(date -u)"
          } > listkr-classic-uBlock.txt
          curl -fsSL "$SRC" \
            | sed 's/\r$//' \
            | grep -v '^!#if' \
            | grep -v '^!#endif' \
            | grep -v '^!#include' \
            | grep -v '^! Title:' \
            | grep -v '^! Description:' \
            | grep -v '^! Version:' \
            | grep -v '^! Expires:' \
            | grep -v '^! Homepage:' \
            | grep -v '^! Licence:' \
            >> listkr-classic-uBlock.txt
          echo "lines: $(wc -l < listkr-classic-uBlock.txt)"

      - name: Commit
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add listkr-classic-uBlock.txt
          if git diff --staged --quiet; then
            echo "No changes"
          else
            git commit -m "Update converted List-KR classic list"
            git push
          fi
